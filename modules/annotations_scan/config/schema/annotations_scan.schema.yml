# annotations_scan has no config entities of its own.
# The annotation_target config entity (owned by the Annotations module) carries all scope and
# annotation data. Its schema lives in annotations/config/schema/annotations.schema.yml.
